# File: .gitattributes

# Set the default behavior to automatically normalize line endings
* text=auto

# Force all text files to use LF in the repo, CRLF on checkout in Windows
*.cs text eol=crlf
*.shader text eol=lf
*.txt text eol=lf
